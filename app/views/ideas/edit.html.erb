<h1>Edit <%= @idea.name %></h1>

<%= form_for [@user, @idea] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :resources %>
  <%= f.text_area :resources %>

  <%= f.submit "Update" %>
  <% end %>
