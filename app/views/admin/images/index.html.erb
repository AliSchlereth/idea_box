<div class="info-details">
  <h1><%= link_to "Add a New Image", new_admin_image_path %></h1>
</div>

<div>
  <% @images.each do |image| %>
    <%=link_to image_tag(image.url, class: "img-thumbnail item-grid"), image_path(image) %>
  <% end %>
</div>
